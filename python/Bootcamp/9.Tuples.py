# tuples are immutable

names =("roger", "syd")

names[0]
names.index("roger")

len(names)

print("Roger" in names)
print(sorted(names))
newTuple = names + ("tina", "quency")
print(newTuple)