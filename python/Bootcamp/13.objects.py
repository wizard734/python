#everything in python is an object 
# even value of basic primitive type is an object

age = 8

print(age.real)
print(age.imag)
print(age.bit_length())

items = [1, 2]
items.append(3)
items.pop()
print(id(items))

#some object are mutable otherwise immutable

